import React from 'react'
import styled from 'styled-components'

const Box = styled.div`
    text-align: center;
    margin: 0 15px;
`

const Timer = () => {
    return (
        <Box>
            
        </Box>
    )
}

export default Timer
